<h1>Add Event</h1>
<%=form_for :event, url: events_path do |f| %>

<p>
	<%= f.label :name %>
	<%= f.text_field :name %><br>
	<%= f.label :date %>
	<%= f.text_area :date %><br>
	<%= f.label :description %>
	<%= f.text_area :description %>

</p>

<p>
	<%= f.submit %>
</p>
<% end %>